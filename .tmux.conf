# set prfix keys (C = CTRL)
set -g prefix C-f

# easy config reload
bind-key r source-file ~/.tmux.conf \; display-message "tmux.conf reloaded."

# mouse mode
set -g mouse on

# use Alt-arrow keys without prefix to switch panes
bind -n M-Left select-pane -L
bind -n M-Right select-pane -R
bind -n M-Up select-pane -U
bind -n M-Down select-pane -D

# Resize panes with Ctrl+Arrow
bind -n C-Left  resize-pane -L 5
bind -n C-Right resize-pane -R 5
bind -n C-Up    resize-pane -U 5
bind -n C-Down  resize-pane -D 5

# set easier window split keys
bind-key v split-window -h
bind-key h split-window -v

# Shift+Arrow to switch windows
bind -n S-Left  previous-window
bind -n S-Right next-window

# easily reorder windows with CTRL+SHIFT+Arrow
bind-key -n C-S-Left swap-window -t -1
bind-key -n C-S-Right swap-window -t +1

# synchronize panes
bind-key S-y set-window-option synchronize-panes\; display-message "synchronize mode toggled."

# easy config reload
bind-key r source-file ~/.tmux.conf \; display-message "tmux.conf reloaded."

# easy clear history
bind-key L clear-history

# key bindings for copy-paste
setw -g mode-keys vi
unbind p
bind p paste-buffer
bind-key -T copy-mode-vi 'v' send -X begin-selection
bind-key -T copy-mode-vi 'y' send -X copy-selection-and-cancel

# Lengthen the amount of time status messages are displayed
set-option -g display-time 2000
set-option -g display-panes-time 2000

# Set the base-index to 1 rather than 0
#set -g base-index 1
#set-window-option -g pane-base-index 1

# Allow the arrow key to be used immediately after changing windows.
set-option -g repeat-time 0

# No delay for escape key press
set -sg escape-time 0

# Theme
set-window-option -g window-status-current-style bold,bg=blue,fg=#000000
set-window-option -g window-status-style fg=#89b4fa
set -g window-status-activity-style bold,bg=#89b4fa,fg=#000000
set-option -g message-style bg=a6adc8,fg=#45475a
set-option -g pane-border-style fg=colour36
set-option -g pane-active-border-style fg=#89b4fa


# Change background color of a tab when activity occurs
setw -g monitor-activity on


# Do NOT reset the color of the tab after activity stops occuring
setw -g monitor-silence 0

# Disable bell
setw -g monitor-bell off

# Disable visual text box when activity occurs
set -g visual-activity off


# Status Bar
set -g status-justify centre
set -g status-bg black
set -g status-fg #89b4fa
set -g status-interval 60
set -g status-left-length 50
set -g status-left "#[bg=#89b4fa,fg=#000000]💻 #[bold]#H#[nobold] [#S] $tmux_target_lower"
set -g status-right "#[bg=#89b4fa,fg=#000000,bold] 🕔 %H:%M "
